<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false"/>
    <uses-permission android:name="android.permission.READ_SMS"/>

    <!--
        Выше указываем на то, что технически нас интересует возможность получать
        доступ до СМС на телефоне пользователя.
        Время, когда запросим у пользователя пермишен, выберем самостоятельно.

        P.S. Это Runtime-пермишен, то что мы его здесь укзаали не значит,
        что нам его уже выдали.
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsReader"
        tools:targetApi="31" >
        <activity
            android:name=".presenter.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>